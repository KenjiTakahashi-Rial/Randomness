<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kenjitakahashirial.randomness">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Randomness"
        android:allowBackup="false">
        <activity android:name=".activities.RandomWordSettingsActivity" />
        <activity android:name=".activities.RandomWordActivity" />
        <activity android:name=".activities.RandomDecimalSettingsActivity" />
        <activity android:name=".activities.RandomDecimalActivity" />
        <activity android:name=".activities.RollDiceSettingsActivity" />
        <activity android:name=".activities.RollDiceActivity" />
        <activity android:name=".activities.FlipCoinSettingsActivity" />
        <activity android:name=".activities.FlipCoinActivity" />
        <activity android:name=".activities.RandomIntegerSettingsActivity" />
        <activity android:name=".activities.RandomIntegerActivity" />
        <activity android:name=".activities.MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>